packages:
  - "apps/*"
  - "packages/*"
  # Benchmark harness. Deliberately OUTSIDE apps/ and packages/ so that
  # provider-specific code cannot reach production by an ordinary import.
  - "benchmark"
