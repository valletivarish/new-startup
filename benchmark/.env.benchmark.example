# Copy values into the repository root `.env` (preferred) or copy this file to
# `.env.benchmark` and source it. Never commit real keys.
#
#   cp benchmark/.env.benchmark.example benchmark/.env.benchmark
#
SARVAM_API_KEY=
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
CARTESIA_API_KEY=
CARTESIA_VOICE_ID=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash-lite
